{% extends "portal/layout.twig" %}
{% block title %}Sign in — SignaturePortal{% endblock %}
{% block body_class %}auth{% endblock %}

{% block topbar %}{% endblock %}

{% block content %}
<div class="card login-card">
    <h1>SignaturePortal</h1>
    <p class="muted">Sign in to manage tenants, templates, and rules.</p>

    {% if error %}
    <div class="alert alert-error">{{ error }}</div>
    {% endif %}

    <form method="post" action="/portal/login" autocomplete="on">
        <input type="hidden" name="_csrf" value="{{ csrf_token }}">

        <label for="email">Email</label>
        <input id="email" name="email" type="email" autocomplete="username" required autofocus>

        <label for="password">Password</label>
        <input id="password" name="password" type="password" autocomplete="current-password" required>

        <button type="submit" class="btn btn-primary">Sign in</button>
    </form>

    <p style="margin-top:12px;"><a href="/portal/password/forgot" class="muted">Forgot password?</a></p>

    {% if sso_tenants is not empty %}
        <div class="login-sep">or</div>
        {% for t in sso_tenants %}
            <a href="/portal/sso/{{ t.slug }}/start" class="btn btn-secondary btn-block">
                Sign in with Microsoft — {{ t.name }}
            </a>
        {% endfor %}
    {% endif %}
</div>
{% endblock %}
